name: CI

on:

  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  call-ci-workflow:
    uses: marksdemoorg/workflow-templates/.github/workflows/marks-demo-org-ci.yml@main
